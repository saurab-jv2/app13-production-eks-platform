cp kubernetes/secrets/grafana-admin-secret.yaml.example \
   kubernetes/secrets/grafana-admin-secret.yaml

# Edit the password then 

kubectl apply -f kubernetes/secrets/grafana-admin-secret.yaml

# store the pwasswords to Secrets and variables in github when using github actions 
# GRAFANA_ADMIN_USER
# GRAFANA_ADMIN_PASSWORD