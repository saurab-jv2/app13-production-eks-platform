TLS setup prepared using cert-manager.

Current stage:
- Self-signed issuer used for learning/demo
- Let’s Encrypt (Route53 DNS-01) planned for production upgrade

Reason HTTPS not enabled:
- No public domain configured yet
- Cluster uses private VPC networking